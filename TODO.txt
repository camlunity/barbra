| реализовать всё, описанное в спеке в главе "Командная строка запуска brb"
  (пока gds, потому что Array.to_list Sys.argv пока что; сделаю позднее.)

| вынести команды ОС, которые могут быть решены чем-то типа fileutils,
  в отдельный модуль.  (mkdir -p, rm -rf, cp -R в основном.)
  (кстати, если кому интересно, как это сделать по всему проекту,
  но "с отсутствующим мозгом" и без grep (не сильно думая,
  рассчитывая только на компилятор) -- обращайтесь // gds.)

| gds: возможно: отрефакторить Bundled (Persistent | Temporary)
  в Bundled | Temporary, так, чтобы потом в Temporary можно было засунуть,
  например, "список ресурсов, которые надо будет подчистить"
  (сейчас в случае tar -xzvf остаётся _dep/tmp/pkgname, а это
  негламурненько.)

| некоторый софт не собирается при make, требует make all -- обсудить
  в чятике, может make all нужен всегда перед make install

| на будущее продумать так, чтобы зависимостям позволялось указывать
  их опции билда -- например, --{enable,disable}-xxx для configure
  или make all vs make all opt для make.  или как-то ещё решить это.
  обсудить и перенести в гуглодоки.

| может стоит ставить в окружении какие-то опции, полезные в том числе
  для оазиса ($prefix), или в configure передавать (вроде отсюда
  оазис их не кушает, зато другие кушают), чтобы софт наподобие
  json-wheel не пытался ставить бинарники в /usr/bin?

| если собираемся показывать 1.0 кому, надо бы английскую документацию
  написать.  но я (gds) не очень хотел бы показывать широко 1.0, если
  моё мнение интересно.
