| barbra.ml: failwith "todo: copy local dir %S" local_path in
| barbra.ml: failwith "todo: copy persistent dir %S to work dir"

| удаление ненужных файлов и директорий (а именно, Bundled Temporary)

| использование формы запуска не command fmt, а Unix.create_process
  или подобного, чтобы не долбаться самим с квотингом путей (сейчас
  квотинга нет нигде, и добавлять его ВЕЗДЕ -- криво.  то есть, будет:
  "command [| "rm" ; "-rf" ; some_path |]"

| потестировать, как собираются проекты с чем-то, требующим .so / ld.conf
  (а именно, байткодовые исполняемые файлы, собранные из зависимостей,
  лежащие в _dep/bin/)

| пропускать установку пакетов, которые уже есть в _dep

| удалять _dep по событиям 1. старт brb, 2. успешный билд всего,
  что затребовали.  В случае неуспешного билда -- необходимо
  оставить _dep так, как она была, чтобы пользователь мог
  а) разобраться с проблемой, б) повторить частичный билд (в будущих
  версиях), в) в случае, если зависимости сбилдились --
  перезапустить make myprog, не перезапуская brb.
